# Local scratch file (gitignored) - not read by anything anymore since the
# Python app was retired. Kept only as a convenient way to push secrets to
# the Supabase Edge Function via:
#   supabase secrets set --project-ref chfqbznodqapxjcqzupj --env-file .env
OPENROUTER_API_KEY=sk-or-...
APOLLO_API_KEY=...
APIFY_API_TOKEN=apify_api_...
ALLOWED_EMAIL=you@example.com
